package com.app.navigation

/**
 * Manifest
 *
 * Layout Inflater и onCreateView более подробно
 *
 * 2.Layout в constructor
 * 3.Отправка результата
 * - Brother - Brother, Parent - child
 *
 *

 * 1. Рассмотреть способы передачи инфы на фрагмент
 * ЖЦ и почему добавилены доп методы. Разделение на Жц фрагмента и вью
 * почему нельзя переопределять конструктор
 * Повторение транзакций после изменения конфигурации if(savedInstanceState != null)
 * retainInstance = true (отключение пересоздания при пересоздании активити) когда нужно использовать (узнать depricated или нет)
 *
 * Реализация планшетной и телефонной верстки
 *
 * Child/Siblings Fragments
 *
 * RetainInstanceFragment
 *
 * Транзакции
 * Типы операций commit
 * Как сбросить стек фрагментов
 * в каких потоках можно работать и как тут вяжется commitAllowingStateLoss
 *
 * StateLoss
 *
 * Одинаковые или разные bundle у активити и фрагмента
 *
 * Расписать работу с бэкстеком и как слушать его

 * */