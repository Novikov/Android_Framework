package com.app.navigation

/**
 * Прочитать всю доку
 *
 * Layout Inflater и onCreateView более подробно
 *
 * 3.Отправка результата
 * - Brother - Brother, Parent - child (это про target fragment)
 * 1. Рассмотреть способы передачи инфы на фрагмент
 * ЖЦ и почему добавилены доп методы. Разделение на Жц фрагмента и вью
 *
 * Реализация планшетной и телефонной верстки
 *
 *
 * Транзакции
 * - реализоват пример с setReorderingAllowed(true), хотя есть комментарий в TransactionsActivity
 * - разобрать как работает Remove + B транзакция (Для этого нужно сделать так чтобы цвета были статическими, а не менялись при каждой транзакции)
 *
 *

setReorderingAllowed(true)
setPrimaryNavigationFragment()
saveBackStack() and restoreBackStack()

Limit the fragment's lifecycle

findFragmentById & by tag
 * */